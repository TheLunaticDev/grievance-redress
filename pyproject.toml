[project]
name = "grievance-redressal"
version = "1.0.0"
description = "A basic grievance-redressal management system."
dependencies = [
    "Flask", "Flask-Mail", "pyotp", "python-dotenv",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
